package ch.fluxed.fluxtrack.data.domain;

/**
 * Urgency level selected by the partner when raising a support ticket
 * (per UC 107 step 2: "Partner selects the urgency via drop-down-menu").
 */
public enum TicketUrgency {
    LOW,
    MEDIUM,
    HIGH
}
